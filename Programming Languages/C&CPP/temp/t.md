### 1. **Mathematical Constants and Special Values**

* **Floating-Point Constants**

  * `HUGE_VAL`, `HUGE_VALF`, `HUGE_VALL`
  * `INFINITY`, `NAN`
* **Floating-Point Classifications**

  * `FP_INFINITE`, `FP_NAN`, `FP_NORMAL`, `FP_SUBNORMAL`, `FP_ZERO`
* **Error Handling**

  * `MATH_ERRNO`, `MATH_ERREXCEPT`
  * `math_errhandling`

---

### 2. **Trigonometric Functions**

* **Basic Trigonometric Functions**

  * `acos()`, `asin()`, `atan()`
  * `cos()`, `sin()`, `tan()`
* **Inverse Trigonometric Functions**

  * `acosf()`, `asinf()`, `atanf()`
  * `cosf()`, `sinf()`, `tanf()`
* **Long Double Versions**

  * `acosl()`, `asinl()`, `atanl()`
  * `cosl()`, `sinl()`, `tanl()`
* **Two-Argument Functions**

  * `atan2()`, `atan2f()`, `atan2l()`

---

### 3. **Hyperbolic Functions**

* **Basic Hyperbolic Functions**

  * `cosh()`, `sinh()`, `tanh()`
* **Inverse Hyperbolic Functions**

  * `acosh()`, `asinh()`, `atanh()`
  * `coshf()`, `sinhf()`, `tanhf()`
  * `acoshl()`, `asinhl()`, `atanhl()`

---

### 4. **Exponential and Logarithmic Functions**

* **Exponential Functions**

  * `exp()`, `expf()`, `expl()`
  * `exp2()`, `exp2f()`, `exp2l()`
  * `expm1()`, `expm1f()`, `expm1l()`
* **Logarithmic Functions**

  * `log()`, `logf()`, `logl()`
  * `log10()`, `log10f()`, `log10l()`
  * `log1p()`, `log1pf()`, `log1pl()`
  * `log2()`, `log2f()`, `log2l()`
* **Logarithmic Utility Functions**

  * `logb()`, `logbf()`, `logbl()`
  * `ilogb()`, `ilogbf()`, `ilogbl()`

---

### 5. **Floating-Point Manipulation**

* **Fractional and Exponent Manipulation**

  * `frexp()`, `frexpf()`, `frexpl()`
  * `ldexp()`, `ldexpf()`, `ldexpl()`
* **Modular and Remainder Functions**

  * `modf()`, `modff()`, `modfl()`
  * `fmod()`, `fmodf()`, `fmodl()`
  * `remainder()`, `remainderf()`, `remainderl()`
  * `remquo()`, `remquof()`, `remquol()`

---

### 6. **Power Functions**

* **Power and Root Functions**

  * `pow()`, `powf()`, `powl()`
  * `sqrt()`, `sqrtf()`, `sqrtl()`
  * `cbrt()`, `cbrtf()`, `cbrtl()`
* **Linear Interpolation**

  * `lerp()`

---

### 7. **Absolute Value Functions**

* **Integer Absolute Functions**

  * `abs()`, `absf()`, `absl()`
* **Floating-Point Absolute Functions**

  * `fabs()`, `fabsf()`, `fabsl()`

---

### 8. **Rounding Functions**

* **Basic Rounding**

  * `ceil()`, `ceilf()`, `ceill()`
  * `floor()`, `floorf()`, `floorl()`
* **Rounding to Nearest Integer**

  * `nearbyint()`, `nearbyintf()`, `nearbyintl()`
  * `rint()`, `rintf()`, `rintl()`
  * `lrint()`, `lrintf()`, `lrintl()`
  * `llrint()`, `llrintf()`, `llrintl()`
  * `round()`, `roundf()`, `roundl()`
  * `lround()`, `lroundf()`, `lroundl()`
  * `llround()`, `llroundf()`, `llroundl()`
* **Truncation Functions**

  * `trunc()`, `truncf()`, `truncl()`

---

### 9. **Sign and Magnitude Functions**

* **Sign Functions**

  * `copysign()`, `copysignf()`, `copysignl()`
* **Floating-Point Comparison**

  * `fpclassify()`
  * `isfinite()`, `isinf()`, `isnan()`, `isnormal()`, `signbit()`
  * `isgreater()`, `isgreaterequal()`, `isless()`, `islessequal()`, `islessgreater()`, `isunordered()`

---

### 10. **Mathematical Special Functions**

* **Gamma and Error Functions**

  * `erf()`, `erff()`, `erfl()`
  * `erfc()`, `erfcf()`, `erfcl()`
  * `lgamma()`, `lgammaf()`, `lgammal()`
  * `tgamma()`, `tgammaf()`, `tgammal()`
* **Bessel Functions**

  * `cyl_bessel_i()`, `cyl_bessel_if()`, `cyl_bessel_il()`
  * `cyl_bessel_j()`, `cyl_bessel_jf()`, `cyl_bessel_jl()`
  * `cyl_bessel_k()`, `cyl_bessel_kf()`, `cyl_bessel_kl()`
  * `cyl_neumann()`, `cyl_neumannf()`, `cyl_neumannl()`
* **Elliptic Integrals**

  * `comp_ellint_1()`, `comp_ellint_1f()`, `comp_ellint_1l()`
  * `comp_ellint_2()`, `comp_ellint_2f()`, `comp_ellint_2l()`
  * `comp_ellint_3()`, `comp_ellint_3f()`, `comp_ellint_3l()`
  * `ellint_1()`, `ellint_1f()`, `ellint_1l()`
  * `ellint_2()`, `ellint_2f()`, `ellint_2l()`
  * `ellint_3()`, `ellint_3f()`, `ellint_3l()`
* **Polynomial Functions**

  * `hermite()`, `hermitef()`, `hermitel()`
  * `laguerre()`, `laguerref()`, `laguerrel()`
  * `legendref()`, `legendrel()`

---

This structure groups the functions into logical categories to help you organize your notes effectively. Let me know if you need any more details or adjustments!

